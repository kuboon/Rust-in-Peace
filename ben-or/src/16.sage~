test@pokaran:~/Rust-in-Peace/ben-or/src$ ./a.out |grep B
poly=B('a^0')*X**0+B('a^1')*X**2+B('a^1')*X**7+B('a^1')*X**9+B('a^1')*X**10+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**1+B('a^1')*X**4+B('a^0')*X**5+B('a^1')*X**8+B('a^0')*X**11+B('a^0')*X**12+B('a^1')*X**13+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**2+B('a^1')*X**3+B('a^0')*X**4+B('a^1')*X**8+B('a^0')*X**9+B('a^0')*X**12+B('a^0')*X**13+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**5+B('a^1')*X**10+B('a^1')*X**11+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**4+B('a^0')*X**5+B('a^1')*X**10+B('a^1')*X**13+B('a^0')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**5+B('a^1')*X**6+B('a^1')*X**7+B('a^1')*X**8+B('a^1')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**2+B('a^1')*X**6+B('a^0')*X**11+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**6+B('a^0')*X**10+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**2+B('a^0')*X**3+B('a^0')*X**4+B('a^0')*X**5+B('a^1')*X**7+B('a^0')*X**9+B('a^0')*X**10+B('a^1')*X**11+B('a^0')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**3+B('a^1')*X**5+B('a^1')*X**6+B('a^0')*X**8+B('a^0')*X**11+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**1+B('a^1')*X**3+B('a^0')*X**4+B('a^0')*X**5+B('a^1')*X**10+B('a^0')*X**11+B('a^0')*X**12+B('a^0')*X**13+B('a^0')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**4+B('a^0')*X**7+B('a^1')*X**9+B('a^0')*X**10+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**3+B('a^0')*X**5+B('a^0')*X**6+B('a^1')*X**7+B('a^0')*X**9+B('a^1')*X**10+B('a^0')*X**11+B('a^0')*X**12+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^1')*X**4+B('a^1')*X**5+B('a^1')*X**11+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^0')*X**2+B('a^0')*X**3+B('a^0')*X**5+B('a^1')*X**6+B('a^1')*X**7+B('a^0')*X**8+B('a^0')*X**11+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**4+B('a^0')*X**7+B('a^0')*X**8+B('a^0')*X**9+B('a^1')*X**13+B('a^0')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**3+B('a^0')*X**4+B('a^0')*X**6+B('a^0')*X**8+B('a^0')*X**9+B('a^0')*X**10+B('a^0')*X**11+B('a^1')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**3+B('a^1')*X**4+B('a^1')*X**6+B('a^1')*X**7+B('a^1')*X**8+B('a^1')*X**12+B('a^1')*X**13+B('a^1')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**4+B('a^0')*X**7+B('a^0')*X**8+B('a^1')*X**9+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**1+B('a^1')*X**2+B('a^1')*X**3+B('a^0')*X**9+B('a^0')*X**10+B('a^0')*X**11+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**3+B('a^0')*X**4+B('a^1')*X**8+B('a^0')*X**11+B('a^1')*X**12+B('a^1')*X**13+B('a^1')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**3+B('a^1')*X**6+B('a^0')*X**9+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**3+B('a^0')*X**6+B('a^0')*X**7+B('a^1')*X**8+B('a^0')*X**12+B('a^1')*X**13+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^1')*X**2+B('a^1')*X**3+B('a^1')*X**4+B('a^1')*X**5+B('a^0')*X**7+B('a^1')*X**8+B('a^1')*X**9+B('a^1')*X**10+B('a^0')*X**11+B('a^0')*X**13+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**3+B('a^1')*X**8+B('a^0')*X**10+B('a^0')*X**11+B('a^0')*X**12+B('a^0')*X**13+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**1+B('a^1')*X**2+B('a^1')*X**3+B('a^1')*X**4+B('a^1')*X**8+B('a^0')*X**9+B('a^0')*X**10+B('a^1')*X**11+B('a^1')*X**12+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**5+B('a^0')*X**6+B('a^0')*X**7+B('a^1')*X**8+B('a^1')*X**11+B('a^1')*X**13+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**2+B('a^1')*X**3+B('a^0')*X**5+B('a^1')*X**6+B('a^0')*X**10+B('a^0')*X**11+B('a^0')*X**12+B('a^1')*X**13+B('a^1')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**1+B('a^1')*X**2+B('a^1')*X**4+B('a^0')*X**5+B('a^1')*X**6+B('a^0')*X**7+B('a^1')*X**8+B('a^1')*X**9+B('a^1')*X**11+B('a^1')*X**13+B('a^0')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**1+B('a^1')*X**2+B('a^1')*X**3+B('a^1')*X**7+B('a^0')*X**10+B('a^1')*X**11+B('a^0')*X**12+B('a^0')*X**13+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**1+B('a^0')*X**5+B('a^1')*X**6+B('a^1')*X**7+B('a^1')*X**8+B('a^1')*X**9+B('a^1')*X**10+B('a^0')*X**13+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**1+B('a^1')*X**3+B('a^0')*X**4+B('a^0')*X**5+B('a^0')*X**6+B('a^1')*X**8+B('a^1')*X**10+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**2+B('a^1')*X**4+B('a^0')*X**5+B('a^1')*X**7+B('a^1')*X**8+B('a^0')*X**10+B('a^1')*X**11+B('a^1')*X**12+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**2+B('a^0')*X**3+B('a^1')*X**8+B('a^0')*X**10+B('a^1')*X**11+B('a^0')*X**13+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^1')*X**7+B('a^0')*X**8+B('a^0')*X**9+B('a^1')*X**11+B('a^1')*X**12+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**2+B('a^0')*X**4+B('a^1')*X**7+B('a^1')*X**8+B('a^0')*X**9+B('a^0')*X**10+B('a^0')*X**13+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**4+B('a^1')*X**5+B('a^0')*X**10+B('a^1')*X**11+B('a^1')*X**12+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**2+B('a^0')*X**6+B('a^1')*X**9+B('a^0')*X**11+B('a^0')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**3+B('a^0')*X**4+B('a^0')*X**5+B('a^1')*X**8+B('a^0')*X**9+B('a^1')*X**12+B('a^1')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^0')*X**4+B('a^0')*X**5+B('a^0')*X**7+B('a^0')*X**10+B('a^1')*X**11+B('a^1')*X**12+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**2+B('a^0')*X**3+B('a^0')*X**4+B('a^0')*X**7+B('a^1')*X**8+B('a^1')*X**9+B('a^1')*X**10+B('a^0')*X**11+B('a^0')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**4+B('a^0')*X**7+B('a^1')*X**10+B('a^0')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**3+B('a^1')*X**5+B('a^0')*X**8+B('a^0')*X**9+B('a^0')*X**11+B('a^1')*X**12+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^0')*X**4+B('a^0')*X**11+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**3+B('a^0')*X**4+B('a^0')*X**6+B('a^1')*X**7+B('a^0')*X**8+B('a^1')*X**10+B('a^0')*X**13+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**2+B('a^0')*X**3+B('a^1')*X**5+B('a^0')*X**6+B('a^0')*X**7+B('a^1')*X**11+B('a^1')*X**12+B('a^1')*X**13+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^1')*X**4+B('a^0')*X**5+B('a^1')*X**7+B('a^1')*X**8+B('a^0')*X**10+B('a^0')*X**13+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**3+B('a^0')*X**5+B('a^1')*X**7+B('a^1')*X**8+B('a^1')*X**10+B('a^0')*X**11+B('a^0')*X**12+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**3+B('a^0')*X**10+B('a^1')*X**11+B('a^0')*X**13+B('a^0')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^0')*X**5+B('a^0')*X**6+B('a^0')*X**9+B('a^0')*X**11+B('a^0')*X**12+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**1+B('a^1')*X**2+B('a^1')*X**4+B('a^0')*X**6+B('a^0')*X**7+B('a^1')*X**8+B('a^1')*X**10+B('a^0')*X**13+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**3+B('a^1')*X**4+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^1')*X**3+B('a^1')*X**8+B('a^1')*X**10+B('a^0')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**3+B('a^1')*X**5+B('a^1')*X**6+B('a^0')*X**7+B('a^0')*X**8+B('a^1')*X**9+B('a^0')*X**10+B('a^0')*X**11+B('a^0')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**2+B('a^0')*X**5+B('a^1')*X**13+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**1+B('a^1')*X**3+B('a^1')*X**4+B('a^1')*X**6+B('a^1')*X**7+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**3+B('a^0')*X**5+B('a^0')*X**6+B('a^1')*X**12+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**3+B('a^0')*X**5+B('a^0')*X**9+B('a^1')*X**10+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**3+B('a^1')*X**5+B('a^1')*X**6+B('a^0')*X**7+B('a^0')*X**8+B('a^0')*X**9+B('a^1')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**2+B('a^1')*X**8+B('a^0')*X**9+B('a^0')*X**12+B('a^1')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**5+B('a^1')*X**7+B('a^1')*X**11+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**1+B('a^1')*X**2+B('a^1')*X**3+B('a^1')*X**6+B('a^1')*X**9+B('a^0')*X**10+B('a^0')*X**13+B('a^1')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**3+B('a^1')*X**4+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**2+B('a^1')*X**3+B('a^0')*X**4+B('a^0')*X**6+B('a^0')*X**10+B('a^1')*X**11+B('a^1')*X**12+B('a^0')*X**13+B('a^0')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**1+B('a^1')*X**3+B('a^1')*X**5+B('a^0')*X**6+B('a^1')*X**8+B('a^0')*X**10+B('a^1')*X**12+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**2+B('a^1')*X**6+B('a^0')*X**7+B('a^0')*X**9+B('a^1')*X**12+B('a^0')*X**13+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**2+B('a^1')*X**3+B('a^1')*X**5+B('a^1')*X**6+B('a^1')*X**7+B('a^0')*X**8+B('a^0')*X**9+B('a^0')*X**10+B('a^0')*X**11+B('a^0')*X**12+B('a^0')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**3+B('a^1')*X**5+B('a^0')*X**6+B('a^0')*X**7+B('a^0')*X**10+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**7+B('a^0')*X**8+B('a^1')*X**9+B('a^1')*X**10+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**2+B('a^0')*X**4+B('a^0')*X**5+B('a^0')*X**11+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**3+B('a^1')*X**4+B('a^1')*X**7+B('a^1')*X**10+B('a^0')*X**11+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^1')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**1+B('a^0')*X**2+B('a^1')*X**3+B('a^0')*X**7+B('a^1')*X**9+B('a^1')*X**11+B('a^0')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**1+B('a^0')*X**3+B('a^0')*X**4+B('a^0')*X**7+B('a^0')*X**8+B('a^0')*X**10+B('a^1')*X**13+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^0')*X**2+B('a^1')*X**3+B('a^1')*X**6+B('a^1')*X**10+B('a^0')*X**11+B('a^0')*X**12+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^1')*X**2+B('a^0')*X**3+B('a^1')*X**4+B('a^0')*X**5+B('a^0')*X**9+B('a^0')*X**11+B('a^0')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**2+B('a^0')*X**6+B('a^1')*X**9+B('a^1')*X**11+B('a^1')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**6+B('a^0')*X**11+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**3+B('a^1')*X**6+B('a^1')*X**11+B('a^1')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**3+B('a^1')*X**8+B('a^1')*X**12+B('a^1')*X**13+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**2+B('a^1')*X**4+B('a^1')*X**7+B('a^1')*X**8+B('a^0')*X**9+B('a^0')*X**10+B('a^0')*X**11+B('a^1')*X**12+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^1')*X**4+B('a^1')*X**5+B('a^1')*X**12+B('a^1')*X**13+B('a^1')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^1')*X**4+B('a^1')*X**6+B('a^1')*X**7+B('a^0')*X**8+B('a^1')*X**9+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^1')*X**2+B('a^0')*X**5+B('a^1')*X**6+B('a^1')*X**7+B('a^1')*X**9+B('a^0')*X**10+B('a^0')*X**11+B('a^1')*X**12+B('a^1')*X**13+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**4+B('a^0')*X**7+B('a^0')*X**9+B('a^1')*X**10+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**1+B('a^0')*X**3+B('a^0')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**2+B('a^0')*X**3+B('a^0')*X**5+B('a^1')*X**8+B('a^0')*X**9+B('a^1')*X**10+B('a^0')*X**11+B('a^1')*X**13+B('a^0')*X**14+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**2+B('a^1')*X**3+B('a^1')*X**6+B('a^0')*X**9+B('a^0')*X**10+B('a^0')*X**13+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**1+B('a^1')*X**3+B('a^0')*X**4+B('a^1')*X**5+B('a^0')*X**6+B('a^0')*X**8+B('a^1')*X**9+B('a^1')*X**10+B('a^1')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**3+B('a^1')*X**7+B('a^1')*X**14+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**1+B('a^0')*X**5+B('a^0')*X**6+B('a^1')*X**7+B('a^0')*X**9+B('a^0')*X**10+B('a^0')*X**13+B('a^0')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^1')*X**1+B('a^1')*X**4+B('a^1')*X**5+B('a^1')*X**7+B('a^1')*X**8+B('a^1')*X**11+B('a^0')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**2+B('a^1')*X**3+B('a^0')*X**7+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**2+B('a^0')*X**3+B('a^1')*X**4+B('a^1')*X**5+B('a^0')*X**6+B('a^0')*X**7+B('a^0')*X**8+B('a^0')*X**12+B('a^1')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**1+B('a^1')*X**2+B('a^1')*X**4+B('a^0')*X**6+B('a^1')*X**10+B('a^1')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**2+B('a^0')*X**8+B('a^0')*X**9+B('a^0')*X**13+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**1+B('a^0')*X**3+B('a^0')*X**7+B('a^0')*X**8+B('a^1')*X**9+B('a^0')*X**10+B('a^0')*X**11+B('a^1')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**3+B('a^1')*X**5+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^1')*X**2+B('a^1')*X**5+B('a^0')*X**7+B('a^0')*X**13+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^1')*X**0+B('a^0')*X**3+B('a^1')*X**5+B('a^1')*X**7+B('a^0')*X**8+B('a^1')*X**11+B('a^0')*X**16+ print(poly.is_irreducible())
poly=B('a^0')*X**0+B('a^0')*X**2+B('a^1')*X**7+B('a^1')*X**8+B('a^0')*X**12+B('a^1')*X**14+B('a^0')*X**15+B('a^0')*X**16+ print(poly.is_irreducible())
test@pokaran:~/Rust-in-Peace/ben-or/src$ exit
exit

Process shell finished
